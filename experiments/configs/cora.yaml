# Cora dataset configuration.
# Homophilous citation network. N≈2485 (LCC), 7 classes, single fixed split.

dataset: cora
n_classes: 7
k: 64
n_layers: 2
lr: 0.01
weight_decay: 0.0005
epochs: 200
loss_strategy: uniform
loss_cutoff: null
seed: 42
n_splits: 1
