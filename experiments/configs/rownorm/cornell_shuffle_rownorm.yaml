# Shuffle diagnostic config — row-norm input (Experiment A)
dataset: cornell
n_classes: 5
k: 64
n_layers: 2
lr: 0.01
weight_decay: 0.0005
epochs: 200
seed: 42
loss_strategy: uniform
loss_cutoff: null
use_row_norm_input: true
use_sphere_norm: true
